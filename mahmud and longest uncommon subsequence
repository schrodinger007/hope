#include <bits/stdc++.h>
using namespace std;
 
int main() {
	// your code goes here
	//char s1[100000],s2[100000];
	//cin>>s1>>s2;
	string a,b;
	  getline (cin, a);
	    getline (cin, b);
	
	if (!a.compare(b))  
        cout<< "-1"; 
  
    
    else cout<<max(a.length(), b.length()); 
	
	return 0;
}
